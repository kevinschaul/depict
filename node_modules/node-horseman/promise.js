var Horseman = require('./');
var horseman = new Horseman();

var path = require('path');

horseman
	.injectJs(path.join(__dirname, './node_modules/bluebird/js/browser/bluebird.js'))
	.on('consoleMessage', function(msg) {
		console.log(msg);
	})
	.on('error', function(err) {
		console.error(err);
	})
	.evaluate(function() {
		'use strict';
		console.log(Promise.config);
	})
	.close();
